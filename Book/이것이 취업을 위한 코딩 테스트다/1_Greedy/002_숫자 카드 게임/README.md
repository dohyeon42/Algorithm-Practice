링크: X

내가 푼 방법: 걍 min을 쓴 리스트의 값을 모은 리스트에 다시 max를 쓰면 되겠다고 생각.
    (
        입력 예시로 실행 한 결과 실행 시간과 메모리 : 
        1. 0.07s 14136KB
        2. 0.1s 14160KB
    )

더 나은 방법(개선): sys.stdin.readline으로 입력을 더욱 빠르게 함. 굳이 리스트 컴프리헨션으로 하지 않고, 굳이 range(m)으로 하지 않고 바로 in해서 조금 코드를 축소시킴.
    (
        입력 예시로 실행 한 결과 실행 시간과 메모리 : 
        1. 0.07s 14160KB
        2. 0.07s 14152KB
    )

배운 점: sys.stdin.readline은 내장 input()보다 입력 양이 많을 때 더욱 차이가 많이 난다. 굳이 리스트 컴프로헨션을 하지 않아도 될때는 하지 않고, 굳이 인덱스를 추가로 사용할 필요가 없을 때는 바로 in하자. 